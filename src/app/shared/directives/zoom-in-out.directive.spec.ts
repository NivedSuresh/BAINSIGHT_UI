import { ZoomInOutDirective } from './zoom-in-out.directive';

describe('ZoomInOutDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomInOutDirective();
    expect(directive).toBeTruthy();
  });
});
